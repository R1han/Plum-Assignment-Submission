# Required for vision extraction and LLM fuzzy classification.
# The deterministic rules engine and the eval harness run without it (OFFLINE_MODE=true).
ANTHROPIC_API_KEY=sk-ant-...

# Optional overrides
# EXTRACTION_MODEL=claude-sonnet-4-6
# ADJUDICATION_MODEL=claude-opus-4-8
# OFFLINE_MODE=false
# CORS_ORIGINS=http://localhost:3000
